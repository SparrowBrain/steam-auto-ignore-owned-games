import { game } from "../types/game";
import { gameToIgnore } from "../types/gameToIgnore";

export const steamGamesToIgnore = (games: game[]): gameToIgnore[] => {
    const gamesToIgnore: gameToIgnore[] = [];
    return gamesToIgnore;
}